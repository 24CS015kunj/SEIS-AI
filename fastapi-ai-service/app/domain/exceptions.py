"""Domain exception hierarchy.

Base application exception plus Business, Validation, Repository,
Embedding, VectorDB, and LLM exceptions, mapped to the error taxonomy
in §14.1 and translated to HTTP responses at the API layer.
"""
