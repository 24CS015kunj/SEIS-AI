"""Shared domain models, enums, and exceptions.

The vocabulary every layer speaks — no dependency on api/, services/,
core/, or infra/, so any module may depend on domain/ without creating
a cycle (§7 Domain Models, §21 AI Constants and Shared Resources).
"""
