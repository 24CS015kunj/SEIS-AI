# Development & test-only dependencies. Never installed in the production
# container image (Task 14 Dockerfile installs requirements.txt only).
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
ruff==0.8.4
mypy==1.14.0
pre-commit==4.0.1
